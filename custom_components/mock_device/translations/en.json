{
  "config": {
    "step": {
      "user": {
        "title": "Mock Device",
        "description": "Create generated synthetic Home Assistant devices for testing. No hardware, account, or helper setup is required."
      }
    },
    "abort": {
      "already_configured": "Mock Device is already configured. v0.1.0 supports one instance to keep entity IDs stable."
    }
  }
}
